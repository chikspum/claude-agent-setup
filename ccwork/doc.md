## Purpose

`ccwork/` is the working area for Claude Code implementation tasks that need a bounded local workspace.

Use this directory for:
- temporary working notes and handoff materials
- generated intermediate files during delegated execution
- task-local artifacts that are useful while work is in progress

Do not keep final product files here unless the task explicitly defines `ccwork/` as the target.
Move accepted outputs into their canonical repo paths before commit.
